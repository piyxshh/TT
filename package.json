{
  "name": "taptalent",
  "version": "1.0.0",
  "private": true,
  "description": "TapTalent — AI Voice Interviewer (LiveKit Agents)",
  "scripts": {
    "dev": "concurrently -n \"backend,frontend,agent\" -c \"blue,green,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"python agent/agent.py dev\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:agent": "python agent/agent.py dev",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend && pip install -r agent/requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
